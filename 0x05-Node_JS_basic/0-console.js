#!/usr/bin/node
/**
 * prints in STDOUT the string argument
 * @param {str} s -  the string argument
 * @return {null} - return nothing
 */

const displayMessage = (s) => {
  console.log(s)
};

module.exports = displayMessage;
